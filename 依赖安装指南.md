# 依赖安装完整指南

## ✅ 问题已解决 - Windows 本地环境

当前 Windows 环境（`d:/CODE/cutImg`）的所有依赖已正确安装：

```
✅ express - 已安装并验证
✅ multer - 已安装并验证
✅ sharp - 已安装并验证
✅ archiver - 已安装并验证
```

**本地环境可以正常运行！**（端口 7788 已被 PM2 占用是正常的）

---

## 🔧 Linux 服务器环境安装指南

### 问题分析

您的错误信息显示：
- 错误路径：`/ql/data/scripts/cutImg/server.js:3`
- 错误信息：`Cannot find module 'sharp'`
- 这是**青龙面板**或其他 Linux 服务器环境

### 解决方案

#### 方法 1：SSH 连接服务器安装（推荐）

```bash
# 1. SSH 连接到服务器
ssh user@your-server-ip

# 2. 进入项目目录
cd /ql/data/scripts/cutImg

# 3. 检查 package.json 是否存在
ls -la package.json

# 4. 安装所有依赖
npm install

# 5. 验证 sharp 安装
node -e "require('sharp'); console.log('✅ sharp 安装成功')"

# 6. 启动服务
npm start
```

#### 方法 2：使用青龙面板 Web 界面

如果是青龙面板环境：

1. **上传项目文件**
   - 确保 `package.json` 已上传到 `/ql/data/scripts/cutImg/`
   - 确保 `server.js` 和 `public/index.html` 都已上传

2. **进入青龙面板容器**
   ```bash
   # Docker 命令（在宿主机执行）
   docker exec -it qinglong bash
   
   # 进入项目目录
   cd /ql/data/scripts/cutImg
   
   # 安装依赖
   npm install
   ```

3. **或使用青龙面板的依赖管理**
   - 进入青龙面板 → 依赖管理
   - 选择 "NodeJs" 类型
   - 分别添加以下依赖：
     - express
     - multer
     - sharp
     - archiver

#### 方法 3：Sharp 特殊处理（如遇到编译问题）

Sharp 是一个原生模块，在某些 Linux 环境可能需要特殊处理：

```bash
# 1. 安装编译工具（Ubuntu/Debian）
sudo apt-get update
sudo apt-get install -y build-essential python3

# 2. 或者使用预编译版本
npm install --platform=linux --arch=x64 sharp

# 3. 或指定版本安装
npm install sharp@0.33.1

# 4. 清除缓存重新安装
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

---

## 📦 完整的 package.json

确保您的 `package.json` 内容如下：

```json
{
  "name": "emoji-cutter",
  "version": "1.0.0",
  "description": "表情包剪切工具 - 自动分割表情版图",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "keywords": ["emoji", "image", "cutter"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "sharp": "^0.33.1",
    "archiver": "^6.0.1"
  }
}
```

---

## 🐛 常见问题排查

### 问题 1：npm 警告 "Unknown builtin config"

```
⚠️ Unknown builtin config "globalignorefile"
⚠️ Unknown builtin config "python"
```

**解决方案：**
```bash
# 这些是警告，不影响安装
# 如需清除，更新 npm 配置
npm config delete globalignorefile
npm config delete python

# 或升级 npm
npm install -g npm@latest
```

### 问题 2：Sharp 安装失败（Linux）

**错误信息：**
```
gyp ERR! build error
node-gyp rebuild
```

**解决方案：**
```bash
# CentOS/RHEL
sudo yum install gcc-c++ make python3

# Ubuntu/Debian
sudo apt-get install -y build-essential python3

# Alpine Linux（Docker）
apk add --no-cache build-base python3

# 然后重新安装
npm install sharp --build-from-source
```

### 问题 3：权限问题

**错误信息：**
```
EACCES: permission denied
```

**解决方案：**
```bash
# 方案1：使用 sudo（不推荐用于生产环境）
sudo npm install

# 方案2：修改 node_modules 权限
sudo chown -R $USER:$USER node_modules
npm install

# 方案3：使用 --unsafe-perm
npm install --unsafe-perm
```

### 问题 4：网络问题（国内服务器）

```bash
# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com
npm install

# 或临时使用
npm install --registry=https://registry.npmmirror.com

# 恢复官方源
npm config set registry https://registry.npmjs.org
```

---

## 🚀 完整安装流程（Linux 服务器）

### 步骤 1：准备工作

```bash
# 连接服务器
ssh user@server-ip

# 确认 Node.js 版本（需要 v14 以上）
node --version

# 确认 npm 版本
npm --version
```

### 步骤 2：上传项目文件

```bash
# 方式1：使用 SCP
scp -r cutImg/* user@server-ip:/ql/data/scripts/cutImg/

# 方式2：使用 Git
cd /ql/data/scripts/cutImg
git clone https://github.com/lylasi/cutImg.git .
```

### 步骤 3：安装依赖

```bash
cd /ql/data/scripts/cutImg

# 检查文件
ls -la

# 应该看到：
# - server.js
# - package.json
# - public/index.html

# 安装依赖（推荐使用淘宝镜像）
npm install --registry=https://registry.npmmirror.com
```

### 步骤 4：验证安装

```bash
# 验证所有依赖
node -e "
const deps = ['express', 'multer', 'sharp', 'archiver'];
deps.forEach(d => {
  try {
    require(d);
    console.log('✅', d, '安装成功');
  } catch(e) {
    console.log('❌', d, '安装失败');
  }
});
"
```

### 步骤 5：创建必要目录

```bash
# 创建上传和输出目录
mkdir -p uploads output public
chmod 755 uploads output public
```

### 步骤 6：测试启动

```bash
# 测试启动（Ctrl+C 停止）
npm start

# 后台运行（使用 PM2）
npm install -g pm2
pm2 start server.js --name cutImg

# 查看日志
pm2 logs cutImg
```

---

## 📝 环境检查清单

运行以下命令检查环境：

```bash
#!/bin/bash

echo "🔍 环境检查开始..."
echo ""

# 检查 Node.js
echo "📦 Node.js 版本:"
node --version || echo "❌ Node.js 未安装"

# 检查 npm
echo ""
echo "📦 npm 版本:"
npm --version || echo "❌ npm 未安装"

# 检查 Python（Sharp 编译需要）
echo ""
echo "🐍 Python 版本:"
python3 --version || python --version || echo "⚠️  Python 未安装（可能影响 Sharp 编译）"

# 检查 gcc（编译工具）
echo ""
echo "🔧 GCC 编译器:"
gcc --version | head -n1 || echo "⚠️  GCC 未安装（可能影响 Sharp 编译）"

# 检查项目文件
echo ""
echo "📁 项目文件:"
[ -f package.json ] && echo "✅ package.json 存在" || echo "❌ package.json 不存在"
[ -f server.js ] && echo "✅ server.js 存在" || echo "❌ server.js 不存在"
[ -d public ] && echo "✅ public 目录存在" || echo "❌ public 目录不存在"

# 检查 node_modules
echo ""
echo "📦 依赖模块:"
[ -d node_modules ] && echo "✅ node_modules 存在" || echo "❌ node_modules 不存在，需要运行 npm install"

# 检查特定依赖
if [ -d node_modules ]; then
  [ -d node_modules/sharp ] && echo "✅ sharp 已安装" || echo "❌ sharp 未安装"
  [ -d node_modules/express ] && echo "✅ express 已安装" || echo "❌ express 未安装"
  [ -d node_modules/multer ] && echo "✅ multer 已安装" || echo "❌ multer 未安装"
  [ -d node_modules/archiver ] && echo "✅ archiver 已安装" || echo "❌ archiver 未安装"
fi

echo ""
echo "✅ 环境检查完成！"
```

保存为 `check-env.sh`，运行：
```bash
chmod +x check-env.sh
./check-env.sh
```

---

## 🎯 快速命令速查

| 操作 | 命令 |
|------|------|
| **安装所有依赖** | `npm install` |
| **清除缓存重装** | `npm cache clean --force && npm install` |
| **使用淘宝镜像** | `npm install --registry=https://registry.npmmirror.com` |
| **验证 Sharp** | `node -e "require('sharp')"` |
| **查看已安装包** | `npm list --depth=0` |
| **重新安装 Sharp** | `npm uninstall sharp && npm install sharp` |
| **启动项目** | `npm start` 或 `node server.js` |
| **后台运行（PM2）** | `pm2 start server.js --name cutImg` |

---

## 💡 最佳实践建议

### 1. 使用 package-lock.json

确保将 `package-lock.json` 一起上传到服务器，保证依赖版本一致：

```bash
# 本地生成
npm install

# 上传到服务器（包含 package-lock.json）
scp package.json package-lock.json user@server:/path/to/project/

# 服务器端安装（会使用 lock 文件）
npm ci  # 更快更可靠
```

### 2. Docker 环境（推荐）

创建 `Dockerfile`：

```dockerfile
FROM node:18-alpine

# 安装 Sharp 所需的依赖
RUN apk add --no-cache \
    build-base \
    g++ \
    cairo-dev \
    jpeg-dev \
    pango-dev \
    giflib-dev

WORKDIR /app

# 复制依赖文件
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制项目文件
COPY . .

# 创建必要目录
RUN mkdir -p uploads output

# 暴露端口
EXPOSE 7788

# 启动命令
CMD ["npm", "start"]
```

构建和运行：
```bash
docker build -t emoji-cutter .
docker run -d -p 7788:7788 --name cutImg emoji-cutter
```

### 3. 持续集成（CI/CD）

`.github/workflows/deploy.yml`：

```yaml
name: Deploy to Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Deploy to server
        run: |
          # 使用 SSH 部署
          ssh user@server "cd /path/to/project && git pull && npm ci && pm2 restart cutImg"
```

---

## 📞 获取帮助

如果仍然遇到问题：

1. **查看完整错误日志**
   ```bash
   npm install --verbose
   ```

2. **检查 Sharp 安装日志**
   ```bash
   npm install sharp --verbose
   ```

3. **在 GitHub 提交 Issue**
   - 包含完整错误信息
   - 系统信息（`uname -a`）
   - Node.js 版本（`node --version`）
   - npm 版本（`npm --version`）

---

**✨ 本地环境已验证可用，服务器环境按上述步骤安装即可！**
